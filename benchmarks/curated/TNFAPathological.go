package curated

import (
	"bytes"
	replace "github.com/KromDaniel/regengo/replace"
	stream "github.com/KromDaniel/regengo/stream"
	"io"
	"strings"
	"sync"
)

// Code generated by regengo for pattern: (?P<outer>(?P<inner>a+)+)b
// DO NOT EDIT.

var tNFAPathologicalStackPool = sync.Pool{New: func() interface{} {
	stack := make([][3]int, 0, 32)
	return &stack
}}

var tNFAPathologicalCaptureStackPool = sync.Pool{New: func() interface{} {
	stack := make([]int, 0, 96)
	return &stack
}}

var tNFAPathologicalVisitedPool = sync.Pool{New: func() interface{} {
	return new([]uint32)
}}

type TNFAPathological struct{}

var CompiledTNFAPathological = TNFAPathological{}

// MinMatchLen is the minimum number of bytes any match can have.
const TNFAPathologicalMinMatchLen = 2

// MaxMatchLen is the maximum number of bytes any match can have.
// -1 means unbounded (pattern contains * or + quantifiers).
const TNFAPathologicalMaxMatchLen = -1

func (TNFAPathological) MatchLengthInfo() (minLen, maxLen int) {
	return TNFAPathologicalMinMatchLen, TNFAPathologicalMaxMatchLen
}

func (TNFAPathological) MatchString(input string) bool {
	l := len(input)
	// Thompson NFA state sets (bitset representation)
	var current uint64
	var next uint64

	// Precomputed constants
	startClosure := uint64(uint64(0xe))
	acceptMask := uint64(uint64(0x200))

	// Epsilon closure lookup for next states after character transitions
	epsilonClosures := [10]uint64{3: uint64(uint64(0x1fc)), 8: uint64(uint64(0x200))}

	// Unanchored pattern - try from each starting position
	for searchStart := 0; searchStart <= l; searchStart++ {
		current = startClosure

		// Check immediate match (empty pattern or already at accept)
		if current&acceptMask != 0 {
			return true
		}

		for offset := searchStart; offset < l; offset++ {
			c := input[offset]
			next = 0

			// State 3
			if current&uint64(uint64(0x8)) != 0 && c == uint8(0x61) {
				next |= epsilonClosures[3]
			}
			// State 8
			if current&uint64(uint64(0x100)) != 0 && c == uint8(0x62) {
				next |= epsilonClosures[8]
			}

			// Update current state set
			current = next

			// Check for dead end
			if current == 0 {
				break
			}

			// Check for match
			if current&acceptMask != 0 {
				return true
			}
		}
	}

	return false
}
func (TNFAPathological) MatchBytes(input []byte) bool {
	l := len(input)
	// Thompson NFA state sets (bitset representation)
	var current uint64
	var next uint64

	// Precomputed constants
	startClosure := uint64(uint64(0xe))
	acceptMask := uint64(uint64(0x200))

	// Epsilon closure lookup for next states after character transitions
	epsilonClosures := [10]uint64{3: uint64(uint64(0x1fc)), 8: uint64(uint64(0x200))}

	// Unanchored pattern - try from each starting position
	for searchStart := 0; searchStart <= l; searchStart++ {
		current = startClosure

		// Check immediate match (empty pattern or already at accept)
		if current&acceptMask != 0 {
			return true
		}

		for offset := searchStart; offset < l; offset++ {
			c := input[offset]
			next = 0

			// State 3
			if current&uint64(uint64(0x8)) != 0 && c == uint8(0x61) {
				next |= epsilonClosures[3]
			}
			// State 8
			if current&uint64(uint64(0x100)) != 0 && c == uint8(0x62) {
				next |= epsilonClosures[8]
			}

			// Update current state set
			current = next

			// Check for dead end
			if current == 0 {
				break
			}

			// Check for match
			if current&acceptMask != 0 {
				return true
			}
		}
	}

	return false
}

// Note: This pattern contains capture groups in repeating/optional context.
// Go's regex engine captures only the LAST match from repeating groups (* + {n,m}).
// For example: (\w)+ matching 'abc' captures 'c', not ['a','b','c'].
// Optional groups (?) return empty string when not matched.

type TNFAPathologicalResult struct {
	Match string // Full match
	Outer string
	Inner string
}

// Note: This pattern contains capture groups in repeating/optional context.
// Go's regex engine captures only the LAST match from repeating groups (* + {n,m}).
// For example: (\w)+ matching 'abc' captures 'c', not ['a','b','c'].
// Optional groups (?) return empty slice when not matched.

type TNFAPathologicalBytesResult struct {
	Match []byte // Full match
	Outer []byte
	Inner []byte
}

func (TNFAPathological) FindStringReuse(input string, r *TNFAPathologicalResult) (*TNFAPathologicalResult, bool) {
	l := len(input)
	offset := 0
	var captures [6]int
	captureStackPtr := tNFAPathologicalCaptureStackPool.Get().(*[]int)
	captureStack := (*captureStackPtr)[:0]
	defer func() {
		*captureStackPtr = captureStack[:0]
		tNFAPathologicalCaptureStackPool.Put(captureStackPtr)
	}()
	stackPtr := tNFAPathologicalStackPool.Get().(*[][3]int)
	stack := (*stackPtr)[:0]
	defer func() {
		for i := range stack {
			stack[i] = [3]int{0, 0, 0}
		}
		*stackPtr = stack[:0]
		tNFAPathologicalStackPool.Put(stackPtr)
	}()
	visitedSize := 10 * (l + 1)
	visitedWords := (visitedSize + 31) / 32
	visitedPtr := tNFAPathologicalVisitedPool.Get().(*[]uint32)
	visited := *visitedPtr
	if cap(visited) < visitedWords {
		visited = make([]uint32, visitedWords)
	} else {
		visited = visited[:visitedWords]
		for i := range visited {
			visited[i] = 0
		}
	}
	defer func() {
		*visitedPtr = visited
		tNFAPathologicalVisitedPool.Put(visitedPtr)
	}()
	captures[0] = 0
	nextInstruction := 1
	goto StepSelect
TryFallback:
	if len(stack) > 0 {
		last := stack[len(stack)-1]
		offset = last[0]
		nextInstruction = last[1]
		stack = stack[:len(stack)-1]
		if last[2] == 1 && len(captureStack) > 0 {
			n := len(captures)
			top := len(captureStack) - n
			copy(captures[:], captureStack[top:])
			captureStack = captureStack[:top]
		}
		goto StepSelect
	} else {
		if l > offset {
			offset++
			for i := range captures {
				captures[i] = 0
			}
			captureStack = captureStack[:0]
			for i := range visited {
				visited[i] = 0
			}
			captures[0] = offset
			nextInstruction = 1
			goto StepSelect
		}
		return nil, false
	}
StepSelect:
	switch nextInstruction {
	case 0:
		goto Ins0
	case 1:
		goto Ins1
	case 2:
		goto Ins2
	case 3:
		goto Ins3
	case 4:
		goto Ins4
	case 5:
		goto Ins5
	case 6:
		goto Ins6
	case 7:
		goto Ins7
	case 8:
		goto Ins8
	case 9:
		goto Ins9
	}
Ins0:
	{
		return nil, false
	}
Ins1:
	{
		captures[2] = offset
		nextInstruction = 2
		goto StepSelect
	}
Ins2:
	{
		captures[4] = offset
		nextInstruction = 3
		goto StepSelect
	}
Ins3:
	{
		if l <= offset {
			goto TryFallback
		}
	}
	{
		if input[offset] != uint8(0x61) {
			goto TryFallback
		}
		offset++
		goto Ins4
	}
Ins4:
	{
		idx := 4*(l+1) + offset
		word, bit := idx/32, uint32(1)<<(idx%32)
		if visited[word]&bit != 0 {
			goto TryFallback
		}
		visited[word] |= bit
		captureStack = append(captureStack, captures[:]...)
		stack = append(stack, [3]int{offset, 5, 1})
		goto Ins3
	}
Ins5:
	{
		captures[5] = offset
		nextInstruction = 6
		goto StepSelect
	}
Ins6:
	{
		idx := 6*(l+1) + offset
		word, bit := idx/32, uint32(1)<<(idx%32)
		if visited[word]&bit != 0 {
			goto TryFallback
		}
		visited[word] |= bit
		captureStack = append(captureStack, captures[:]...)
		stack = append(stack, [3]int{offset, 7, 1})
		goto Ins2
	}
Ins7:
	{
		captures[3] = offset
		nextInstruction = 8
		goto StepSelect
	}
Ins8:
	{
		if l <= offset {
			goto TryFallback
		}
	}
	{
		if input[offset] != uint8(0x62) {
			goto TryFallback
		}
		offset++
		goto Ins9
	}
Ins9:
	{
		captures[1] = offset
		if r == nil {
			r = &TNFAPathologicalResult{}
		}
		r.Match = string(input[captures[0]:captures[1]])
		if captures[2] <= captures[3] && captures[3] <= len(input) {
			r.Outer = string(input[captures[2]:captures[3]])
		} else {
			r.Outer = ""
		}
		if captures[4] <= captures[5] && captures[5] <= len(input) {
			r.Inner = string(input[captures[4]:captures[5]])
		} else {
			r.Inner = ""
		}
		return r, true
	}
}
func (recv TNFAPathological) FindString(input string) (*TNFAPathologicalResult, bool) {
	return recv.FindStringReuse(input, nil)
}
func (TNFAPathological) FindAllStringAppend(input string, n int, s []*TNFAPathologicalResult) []*TNFAPathologicalResult {
	if n == 0 {
		return s
	}
	result := s
	l := len(input)
	searchStart := 0
	captureStackPtr := tNFAPathologicalCaptureStackPool.Get().(*[]int)
	captureStack := (*captureStackPtr)[:0]
	defer func() {
		*captureStackPtr = captureStack[:0]
		tNFAPathologicalCaptureStackPool.Put(captureStackPtr)
	}()
	stackPtr := tNFAPathologicalStackPool.Get().(*[][3]int)
	stack := (*stackPtr)[:0]
	defer func() {
		for i := range stack {
			stack[i] = [3]int{0, 0, 0}
		}
		*stackPtr = stack[:0]
		tNFAPathologicalStackPool.Put(stackPtr)
	}()
	visitedSize := 10 * (l + 1)
	visitedWords := (visitedSize + 31) / 32
	visitedPtr := tNFAPathologicalVisitedPool.Get().(*[]uint32)
	visited := *visitedPtr
	if cap(visited) < visitedWords {
		visited = make([]uint32, visitedWords)
	} else {
		visited = visited[:visitedWords]
		for i := range visited {
			visited[i] = 0
		}
	}
	defer func() {
		*visitedPtr = visited
		tNFAPathologicalVisitedPool.Put(visitedPtr)
	}()
	for true {
		if n > 0 && len(result) >= n {
			break
		}
		if searchStart >= l {
			break
		}
		offset := searchStart
		var captures [6]int
		captureStack = captureStack[:0]
		stack = stack[:0]
		captures[0] = searchStart
		nextInstruction := 1
		goto StepSelect
	TryFallback:
		if len(stack) > 0 {
			last := stack[len(stack)-1]
			offset = last[0]
			nextInstruction = last[1]
			stack = stack[:len(stack)-1]
			if last[2] == 1 && len(captureStack) > 0 {
				n := len(captures)
				top := len(captureStack) - n
				copy(captures[:], captureStack[top:])
				captureStack = captureStack[:top]
			}
			goto StepSelect
		} else {
			searchStart++
			continue
		}
	StepSelect:
		switch nextInstruction {
		case 0:
			goto Ins0
		case 1:
			goto Ins1
		case 2:
			goto Ins2
		case 3:
			goto Ins3
		case 4:
			goto Ins4
		case 5:
			goto Ins5
		case 6:
			goto Ins6
		case 7:
			goto Ins7
		case 8:
			goto Ins8
		case 9:
			goto Ins9
		}
	Ins0:
		{
			goto TryFallback
		}
	Ins1:
		{
			captures[2] = offset
			nextInstruction = 2
			goto StepSelect
		}
	Ins2:
		{
			captures[4] = offset
			nextInstruction = 3
			goto StepSelect
		}
	Ins3:
		{
			if l <= offset {
				goto TryFallback
			}
		}
		{
			if input[offset] != uint8(0x61) {
				goto TryFallback
			}
			offset++
			goto Ins4
		}
	Ins4:
		{
			idx := 4*(l+1) + offset
			word, bit := idx/32, uint32(1)<<(idx%32)
			if visited[word]&bit != 0 {
				goto TryFallback
			}
			visited[word] |= bit
			captureStack = append(captureStack, captures[:]...)
			stack = append(stack, [3]int{offset, 5, 1})
			goto Ins3
		}
	Ins5:
		{
			captures[5] = offset
			nextInstruction = 6
			goto StepSelect
		}
	Ins6:
		{
			idx := 6*(l+1) + offset
			word, bit := idx/32, uint32(1)<<(idx%32)
			if visited[word]&bit != 0 {
				goto TryFallback
			}
			visited[word] |= bit
			captureStack = append(captureStack, captures[:]...)
			stack = append(stack, [3]int{offset, 7, 1})
			goto Ins2
		}
	Ins7:
		{
			captures[3] = offset
			nextInstruction = 8
			goto StepSelect
		}
	Ins8:
		{
			if l <= offset {
				goto TryFallback
			}
		}
		{
			if input[offset] != uint8(0x62) {
				goto TryFallback
			}
			offset++
			goto Ins9
		}
	Ins9:
		{
			captures[1] = offset
			var item *TNFAPathologicalResult
			if len(result) < cap(result) {
				result = result[:len(result)+1]
				item = result[len(result)-1]
				if item == nil {
					item = &TNFAPathologicalResult{}
					result[len(result)-1] = item
				}
			} else {
				item = &TNFAPathologicalResult{}
				result = append(result, item)
			}
			item.Match = string(input[captures[0]:captures[1]])
			if captures[2] <= captures[3] && captures[3] <= len(input) {
				item.Outer = string(input[captures[2]:captures[3]])
			} else {
				item.Outer = ""
			}
			if captures[4] <= captures[5] && captures[5] <= len(input) {
				item.Inner = string(input[captures[4]:captures[5]])
			} else {
				item.Inner = ""
			}
			if captures[1] > searchStart {
				searchStart = captures[1]
			} else {
				searchStart++
			}
			continue
		}
	}
	return result
}
func (r TNFAPathological) FindAllString(input string, n int) []*TNFAPathologicalResult {
	return r.FindAllStringAppend(input, n, nil)
}
func (TNFAPathological) FindBytesReuse(input []byte, r *TNFAPathologicalBytesResult) (*TNFAPathologicalBytesResult, bool) {
	l := len(input)
	offset := 0
	var captures [6]int
	captureStackPtr := tNFAPathologicalCaptureStackPool.Get().(*[]int)
	captureStack := (*captureStackPtr)[:0]
	defer func() {
		*captureStackPtr = captureStack[:0]
		tNFAPathologicalCaptureStackPool.Put(captureStackPtr)
	}()
	stackPtr := tNFAPathologicalStackPool.Get().(*[][3]int)
	stack := (*stackPtr)[:0]
	defer func() {
		for i := range stack {
			stack[i] = [3]int{0, 0, 0}
		}
		*stackPtr = stack[:0]
		tNFAPathologicalStackPool.Put(stackPtr)
	}()
	visitedSize := 10 * (l + 1)
	visitedWords := (visitedSize + 31) / 32
	visitedPtr := tNFAPathologicalVisitedPool.Get().(*[]uint32)
	visited := *visitedPtr
	if cap(visited) < visitedWords {
		visited = make([]uint32, visitedWords)
	} else {
		visited = visited[:visitedWords]
		for i := range visited {
			visited[i] = 0
		}
	}
	defer func() {
		*visitedPtr = visited
		tNFAPathologicalVisitedPool.Put(visitedPtr)
	}()
	captures[0] = 0
	nextInstruction := 1
	goto StepSelect
TryFallback:
	if len(stack) > 0 {
		last := stack[len(stack)-1]
		offset = last[0]
		nextInstruction = last[1]
		stack = stack[:len(stack)-1]
		if last[2] == 1 && len(captureStack) > 0 {
			n := len(captures)
			top := len(captureStack) - n
			copy(captures[:], captureStack[top:])
			captureStack = captureStack[:top]
		}
		goto StepSelect
	} else {
		if l > offset {
			offset++
			for i := range captures {
				captures[i] = 0
			}
			captureStack = captureStack[:0]
			for i := range visited {
				visited[i] = 0
			}
			captures[0] = offset
			nextInstruction = 1
			goto StepSelect
		}
		return nil, false
	}
StepSelect:
	switch nextInstruction {
	case 0:
		goto Ins0
	case 1:
		goto Ins1
	case 2:
		goto Ins2
	case 3:
		goto Ins3
	case 4:
		goto Ins4
	case 5:
		goto Ins5
	case 6:
		goto Ins6
	case 7:
		goto Ins7
	case 8:
		goto Ins8
	case 9:
		goto Ins9
	}
Ins0:
	{
		return nil, false
	}
Ins1:
	{
		captures[2] = offset
		nextInstruction = 2
		goto StepSelect
	}
Ins2:
	{
		captures[4] = offset
		nextInstruction = 3
		goto StepSelect
	}
Ins3:
	{
		if l <= offset {
			goto TryFallback
		}
	}
	{
		if input[offset] != uint8(0x61) {
			goto TryFallback
		}
		offset++
		goto Ins4
	}
Ins4:
	{
		idx := 4*(l+1) + offset
		word, bit := idx/32, uint32(1)<<(idx%32)
		if visited[word]&bit != 0 {
			goto TryFallback
		}
		visited[word] |= bit
		captureStack = append(captureStack, captures[:]...)
		stack = append(stack, [3]int{offset, 5, 1})
		goto Ins3
	}
Ins5:
	{
		captures[5] = offset
		nextInstruction = 6
		goto StepSelect
	}
Ins6:
	{
		idx := 6*(l+1) + offset
		word, bit := idx/32, uint32(1)<<(idx%32)
		if visited[word]&bit != 0 {
			goto TryFallback
		}
		visited[word] |= bit
		captureStack = append(captureStack, captures[:]...)
		stack = append(stack, [3]int{offset, 7, 1})
		goto Ins2
	}
Ins7:
	{
		captures[3] = offset
		nextInstruction = 8
		goto StepSelect
	}
Ins8:
	{
		if l <= offset {
			goto TryFallback
		}
	}
	{
		if input[offset] != uint8(0x62) {
			goto TryFallback
		}
		offset++
		goto Ins9
	}
Ins9:
	{
		captures[1] = offset
		if r == nil {
			r = &TNFAPathologicalBytesResult{}
		}
		r.Match = input[captures[0]:captures[1]]
		if captures[2] <= captures[3] && captures[3] <= len(input) {
			r.Outer = input[captures[2]:captures[3]]
		} else {
			r.Outer = nil
		}
		if captures[4] <= captures[5] && captures[5] <= len(input) {
			r.Inner = input[captures[4]:captures[5]]
		} else {
			r.Inner = nil
		}
		return r, true
	}
}
func (recv TNFAPathological) FindBytes(input []byte) (*TNFAPathologicalBytesResult, bool) {
	return recv.FindBytesReuse(input, nil)
}
func (TNFAPathological) FindAllBytesAppend(input []byte, n int, s []*TNFAPathologicalBytesResult) []*TNFAPathologicalBytesResult {
	if n == 0 {
		return s
	}
	result := s
	l := len(input)
	searchStart := 0
	captureStackPtr := tNFAPathologicalCaptureStackPool.Get().(*[]int)
	captureStack := (*captureStackPtr)[:0]
	defer func() {
		*captureStackPtr = captureStack[:0]
		tNFAPathologicalCaptureStackPool.Put(captureStackPtr)
	}()
	stackPtr := tNFAPathologicalStackPool.Get().(*[][3]int)
	stack := (*stackPtr)[:0]
	defer func() {
		for i := range stack {
			stack[i] = [3]int{0, 0, 0}
		}
		*stackPtr = stack[:0]
		tNFAPathologicalStackPool.Put(stackPtr)
	}()
	visitedSize := 10 * (l + 1)
	visitedWords := (visitedSize + 31) / 32
	visitedPtr := tNFAPathologicalVisitedPool.Get().(*[]uint32)
	visited := *visitedPtr
	if cap(visited) < visitedWords {
		visited = make([]uint32, visitedWords)
	} else {
		visited = visited[:visitedWords]
		for i := range visited {
			visited[i] = 0
		}
	}
	defer func() {
		*visitedPtr = visited
		tNFAPathologicalVisitedPool.Put(visitedPtr)
	}()
	for true {
		if n > 0 && len(result) >= n {
			break
		}
		if searchStart >= l {
			break
		}
		offset := searchStart
		var captures [6]int
		captureStack = captureStack[:0]
		stack = stack[:0]
		captures[0] = searchStart
		nextInstruction := 1
		goto StepSelect
	TryFallback:
		if len(stack) > 0 {
			last := stack[len(stack)-1]
			offset = last[0]
			nextInstruction = last[1]
			stack = stack[:len(stack)-1]
			if last[2] == 1 && len(captureStack) > 0 {
				n := len(captures)
				top := len(captureStack) - n
				copy(captures[:], captureStack[top:])
				captureStack = captureStack[:top]
			}
			goto StepSelect
		} else {
			searchStart++
			continue
		}
	StepSelect:
		switch nextInstruction {
		case 0:
			goto Ins0
		case 1:
			goto Ins1
		case 2:
			goto Ins2
		case 3:
			goto Ins3
		case 4:
			goto Ins4
		case 5:
			goto Ins5
		case 6:
			goto Ins6
		case 7:
			goto Ins7
		case 8:
			goto Ins8
		case 9:
			goto Ins9
		}
	Ins0:
		{
			goto TryFallback
		}
	Ins1:
		{
			captures[2] = offset
			nextInstruction = 2
			goto StepSelect
		}
	Ins2:
		{
			captures[4] = offset
			nextInstruction = 3
			goto StepSelect
		}
	Ins3:
		{
			if l <= offset {
				goto TryFallback
			}
		}
		{
			if input[offset] != uint8(0x61) {
				goto TryFallback
			}
			offset++
			goto Ins4
		}
	Ins4:
		{
			idx := 4*(l+1) + offset
			word, bit := idx/32, uint32(1)<<(idx%32)
			if visited[word]&bit != 0 {
				goto TryFallback
			}
			visited[word] |= bit
			captureStack = append(captureStack, captures[:]...)
			stack = append(stack, [3]int{offset, 5, 1})
			goto Ins3
		}
	Ins5:
		{
			captures[5] = offset
			nextInstruction = 6
			goto StepSelect
		}
	Ins6:
		{
			idx := 6*(l+1) + offset
			word, bit := idx/32, uint32(1)<<(idx%32)
			if visited[word]&bit != 0 {
				goto TryFallback
			}
			visited[word] |= bit
			captureStack = append(captureStack, captures[:]...)
			stack = append(stack, [3]int{offset, 7, 1})
			goto Ins2
		}
	Ins7:
		{
			captures[3] = offset
			nextInstruction = 8
			goto StepSelect
		}
	Ins8:
		{
			if l <= offset {
				goto TryFallback
			}
		}
		{
			if input[offset] != uint8(0x62) {
				goto TryFallback
			}
			offset++
			goto Ins9
		}
	Ins9:
		{
			captures[1] = offset
			var item *TNFAPathologicalBytesResult
			if len(result) < cap(result) {
				result = result[:len(result)+1]
				item = result[len(result)-1]
				if item == nil {
					item = &TNFAPathologicalBytesResult{}
					result[len(result)-1] = item
				}
			} else {
				item = &TNFAPathologicalBytesResult{}
				result = append(result, item)
			}
			item.Match = input[captures[0]:captures[1]]
			if captures[2] <= captures[3] && captures[3] <= len(input) {
				item.Outer = input[captures[2]:captures[3]]
			} else {
				item.Outer = nil
			}
			if captures[4] <= captures[5] && captures[5] <= len(input) {
				item.Inner = input[captures[4]:captures[5]]
			} else {
				item.Inner = nil
			}
			if captures[1] > searchStart {
				searchStart = captures[1]
			} else {
				searchStart++
			}
			continue
		}
	}
	return result
}
func (r TNFAPathological) FindAllBytes(input []byte, n int) []*TNFAPathologicalBytesResult {
	return r.FindAllBytesAppend(input, n, nil)
}

// DefaultMaxLeftover returns the recommended MaxLeftover value for streaming.
// For bounded patterns, this is 10 * MaxMatchLen.
// For unbounded patterns, this returns 1MB as a safety limit.
func (TNFAPathological) DefaultMaxLeftover() int {
	return 1048576
}

// FindReader streams matches from an io.Reader.
//
// The onMatch callback is called for each match found. Return false to stop
// processing early. The StreamMatch.Result points into an internal buffer
// and is only valid during the callback - copy if needed.
//
// Returns nil on success (including early termination via callback).
// Returns error on read failure.
func (TNFAPathological) FindReader(r io.Reader, cfg stream.Config, onMatch func(stream.Match[*TNFAPathologicalBytesResult]) bool) error {
	if err := cfg.Validate(65536); err != nil {
		return err
	}
	cfg = cfg.ApplyDefaults(65536, 1048576)

	buf := make([]byte, cfg.BufferSize)
	leftover := 0
	streamOffset := int64(0)
	chunkIndex := 0

	reuseResult := &TNFAPathologicalBytesResult{}

	for {
		n, err := r.Read(buf[leftover:])
		if n == 0 && err != nil {
			if err == io.EOF {
				// Process any remaining data in leftover
				if leftover > 0 {
					chunk := buf[:leftover]
					searchPos := 0
					for searchPos < len(chunk) {
						result, ok := TNFAPathological{}.FindBytesReuse(chunk[searchPos:], reuseResult)
						if !ok {
							break
						}
						matchIdx := bytes.Index(chunk[searchPos:], result.Match)
						if matchIdx < 0 {
							break
						}
						matchStart := searchPos + matchIdx

						m := stream.Match[*TNFAPathologicalBytesResult]{
							ChunkIndex:   chunkIndex,
							Result:       result,
							StreamOffset: streamOffset + int64(matchStart),
						}
						if !onMatch(m) {
							return nil
						}
						if len(result.Match) > 0 {
							searchPos = matchStart + len(result.Match)
						} else {
							searchPos++
						}
					}
				}
				return nil
			}
			return err
		}

		dataLen := leftover + n
		chunk := buf[:dataLen]
		isFull := n == cfg.BufferSize-leftover

		searchPos := 0
		committed := 0
		for searchPos < len(chunk) {
			result, ok := TNFAPathological{}.FindBytesReuse(chunk[searchPos:], reuseResult)
			if !ok {
				break
			}
			matchIdx := bytes.Index(chunk[searchPos:], result.Match)
			if matchIdx < 0 {
				break
			}
			matchStart := searchPos + matchIdx
			matchEnd := matchStart + len(result.Match)

			if isFull && matchEnd > dataLen-cfg.MaxLeftover {
				// Match too close to boundary, defer to next chunk
				break
			}

			m := stream.Match[*TNFAPathologicalBytesResult]{
				ChunkIndex:   chunkIndex,
				Result:       result,
				StreamOffset: streamOffset + int64(matchStart),
			}
			if !onMatch(m) {
				return nil
			}
			committed = matchEnd
			if len(result.Match) > 0 {
				searchPos = matchEnd
			} else {
				searchPos++
			}
		}

		// Prepare leftover for next iteration
		if isFull {
			// Buffer was full, need to keep leftover
			keepFrom := dataLen - cfg.MaxLeftover
			if keepFrom < committed {
				keepFrom = committed
			}
			leftover = dataLen - keepFrom
			streamOffset += int64(keepFrom)
			copy(buf[:leftover], buf[keepFrom:dataLen])
		} else {
			// Reached EOF or short read, no more data
			leftover = 0
		}
		chunkIndex++
		if err == io.EOF {
			return nil
		}
	}
}

// FindReaderCount counts matches without allocating result structs.
// More efficient when you only need the count.
func (TNFAPathological) FindReaderCount(r io.Reader, cfg stream.Config) (int64, error) {
	var count int64
	err := TNFAPathological{}.FindReader(r, cfg, func(_ stream.Match[*TNFAPathologicalBytesResult]) bool {
		count++
		return true
	})
	return count, err
}

// FindReaderFirst returns the first match, or nil if none found.
// The returned result is a copy (safe to use after call returns).
func (TNFAPathological) FindReaderFirst(r io.Reader, cfg stream.Config) (*TNFAPathologicalBytesResult, int64, error) {
	var result *TNFAPathologicalBytesResult
	var offset int64
	err := TNFAPathological{}.FindReader(r, cfg, func(m stream.Match[*TNFAPathologicalBytesResult]) bool {
		// Copy the result since buffer will be reused
		result = TNFAPathological{}.copyBytesResult(m.Result)
		offset = m.StreamOffset
		return false
	})
	return result, offset, err
}

// copyBytesResult creates a deep copy of a BytesResult.
// This is needed because the original slices point into the stream buffer.
func (TNFAPathological) copyBytesResult(src *TNFAPathologicalBytesResult) *TNFAPathologicalBytesResult {
	if src == nil {
		return nil
	}
	dst := &TNFAPathologicalBytesResult{}
	// Copy Match slice
	dst.Match = append([]byte{}, src.Match...)
	if src.Outer != nil {
		dst.Outer = append([]byte{}, src.Outer...)
	}
	if src.Inner != nil {
		dst.Inner = append([]byte{}, src.Inner...)
	}
	return dst
}

// CaptureByIndex returns the capture group value by its 0-based index.
// Index 0 returns the full match, 1+ returns capture groups.
func (r *TNFAPathologicalResult) CaptureByIndex(idx int) string {
	switch idx {
	case 0:
		return r.Match
	case 1:
		return r.Outer
	case 2:
		return r.Inner
	default:
		return ""
	}
}

// CaptureByIndex returns the capture group value by its 0-based index.
// Index 0 returns the full match, 1+ returns capture groups.
func (r *TNFAPathologicalBytesResult) CaptureByIndex(idx int) []byte {
	switch idx {
	case 0:
		return r.Match
	case 1:
		return r.Outer
	case 2:
		return r.Inner
	default:
		return nil
	}
}

// ReplaceAllString replaces all matches in input with the template expansion.
// Template syntax: $0 (full match), $1/$2 (by index), $name (by name), $$ (literal $)
func (TNFAPathological) ReplaceAllString(input string, template string) string {
	tmpl, err := replace.Parse(template)
	if err != nil {
		return input
	}

	var result strings.Builder
	lastEnd := 0
	var r TNFAPathologicalResult

	remaining := input
	offset := 0

	for {
		match, ok := TNFAPathological{}.FindStringReuse(remaining, &r)
		if !ok {
			break
		}

		matchIdx := strings.Index(remaining, match.Match)
		if matchIdx < 0 {
			break
		}

		matchStart := offset + matchIdx
		matchEnd := matchStart + len(match.Match)

		result.WriteString(input[lastEnd:matchStart])

		for _, seg := range tmpl.Segments {
			switch seg.Type {
			case replace.SegmentLiteral:
				result.WriteString(seg.Literal)
			case replace.SegmentFullMatch:
				result.WriteString(match.Match)
			case replace.SegmentCaptureIndex:
				result.WriteString(match.CaptureByIndex(seg.CaptureIndex))
			case replace.SegmentCaptureName:
				switch seg.CaptureName {
				case "outer":
					result.WriteString(match.Outer)
				case "inner":
					result.WriteString(match.Inner)
				}
			}
		}

		lastEnd = matchEnd
		if len(match.Match) > 0 {
			remaining = input[matchEnd:]
			offset = matchEnd
		} else {
			if matchEnd < len(input) {
				remaining = input[matchEnd+1:]
				offset = matchEnd + 1
			} else {
				break
			}
		}
	}

	result.WriteString(input[lastEnd:])
	return result.String()
}

// ReplaceAllBytes replaces all matches in input with the template expansion.
// Template syntax: $0 (full match), $1/$2 (by index), $name (by name), $$ (literal $)
func (TNFAPathological) ReplaceAllBytes(input []byte, template string) []byte {
	return TNFAPathological{}.ReplaceAllBytesAppend(input, template, nil)
}

// ReplaceAllBytesAppend replaces all matches and appends to buf.
// If buf has sufficient capacity, no allocation occurs.
// Template syntax: $0 (full match), $1/$2 (by index), $name (by name), $$ (literal $)
func (TNFAPathological) ReplaceAllBytesAppend(input []byte, template string, buf []byte) []byte {
	tmpl, err := replace.Parse(template)
	if err != nil {
		return append(buf, input...)
	}

	result := buf[:0]
	lastEnd := 0
	var r TNFAPathologicalBytesResult

	remaining := input
	offset := 0

	for {
		match, ok := TNFAPathological{}.FindBytesReuse(remaining, &r)
		if !ok {
			break
		}

		matchIdx := bytes.Index(remaining, match.Match)
		if matchIdx < 0 {
			break
		}

		matchStart := offset + matchIdx
		matchEnd := matchStart + len(match.Match)

		result = append(result, input[lastEnd:matchStart]...)

		for _, seg := range tmpl.Segments {
			switch seg.Type {
			case replace.SegmentLiteral:
				result = append(result, seg.Literal...)
			case replace.SegmentFullMatch:
				result = append(result, match.Match...)
			case replace.SegmentCaptureIndex:
				result = append(result, match.CaptureByIndex(seg.CaptureIndex)...)
			case replace.SegmentCaptureName:
				switch seg.CaptureName {
				case "outer":
					result = append(result, match.Outer...)
				case "inner":
					result = append(result, match.Inner...)
				}
			}
		}

		lastEnd = matchEnd
		if len(match.Match) > 0 {
			remaining = input[matchEnd:]
			offset = matchEnd
		} else {
			if matchEnd < len(input) {
				remaining = input[matchEnd+1:]
				offset = matchEnd + 1
			} else {
				break
			}
		}
	}

	result = append(result, input[lastEnd:]...)
	return result
}

// ReplaceFirstString replaces only the first match in input with the template expansion.
// Template syntax: $0 (full match), $1/$2 (by index), $name (by name), $$ (literal $)
func (TNFAPathological) ReplaceFirstString(input string, template string) string {
	tmpl, err := replace.Parse(template)
	if err != nil {
		return input
	}

	var r TNFAPathologicalResult
	match, ok := TNFAPathological{}.FindStringReuse(input, &r)
	if !ok {
		return input
	}

	matchIdx := strings.Index(input, match.Match)
	if matchIdx < 0 {
		return input
	}

	var result strings.Builder
	result.WriteString(input[:matchIdx])

	for _, seg := range tmpl.Segments {
		switch seg.Type {
		case replace.SegmentLiteral:
			result.WriteString(seg.Literal)
		case replace.SegmentFullMatch:
			result.WriteString(match.Match)
		case replace.SegmentCaptureIndex:
			result.WriteString(match.CaptureByIndex(seg.CaptureIndex))
		case replace.SegmentCaptureName:
			switch seg.CaptureName {
			case "outer":
				result.WriteString(match.Outer)
			case "inner":
				result.WriteString(match.Inner)
			}
		}
	}

	result.WriteString(input[matchIdx+len(match.Match):])
	return result.String()
}

// ReplaceFirstBytes replaces only the first match in input with the template expansion.
// Template syntax: $0 (full match), $1/$2 (by index), $name (by name), $$ (literal $)
func (TNFAPathological) ReplaceFirstBytes(input []byte, template string) []byte {
	tmpl, err := replace.Parse(template)
	if err != nil {
		return append([]byte{}, input...)
	}

	var r TNFAPathologicalBytesResult
	match, ok := TNFAPathological{}.FindBytesReuse(input, &r)
	if !ok {
		return append([]byte{}, input...)
	}

	matchIdx := bytes.Index(input, match.Match)
	if matchIdx < 0 {
		return append([]byte{}, input...)
	}

	var result []byte
	result = append(result, input[:matchIdx]...)

	for _, seg := range tmpl.Segments {
		switch seg.Type {
		case replace.SegmentLiteral:
			result = append(result, seg.Literal...)
		case replace.SegmentFullMatch:
			result = append(result, match.Match...)
		case replace.SegmentCaptureIndex:
			result = append(result, match.CaptureByIndex(seg.CaptureIndex)...)
		case replace.SegmentCaptureName:
			switch seg.CaptureName {
			case "outer":
				result = append(result, match.Outer...)
			case "inner":
				result = append(result, match.Inner...)
			}
		}
	}

	result = append(result, input[matchIdx+len(match.Match):]...)
	return result
}
